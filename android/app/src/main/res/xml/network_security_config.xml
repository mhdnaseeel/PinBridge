<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration (V-17)
    - Blocks all cleartext (HTTP) traffic
    - Trusts only system CA certificates
    - Prevents custom/corporate proxy CAs from intercepting traffic in production
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for local development only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
